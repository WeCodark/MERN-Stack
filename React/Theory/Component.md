# Component (JSX)
* A component is just a JS function whose name starts with a capital letter and returns UI Code

* JSX is the mixture of HTML and JavaScript
    * use {} to insert JavaScript variables inside HTML
    * use className instead of Class

```function Greetings(){
    const name = 'Aryan';
    return <h1>Hello {name}</h1> // JSX
}```